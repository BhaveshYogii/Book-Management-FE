
version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - cd Team3-Book-Management-FE
      - npm install
      - npm i aos
  build:
    commands:
      - echo Build started on 'date'
      - npm run build
artifacts:
  files:
    - '**/*'
    #- "my-app/build/**/*"
  base-directory: '/dist'
  #discard-paths: yes
